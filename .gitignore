# Sesiones WhatsApp — archivos generados automáticamente por Baileys
src/auth/cuenta-*/
src/auth/*.png

# Logs de errores
log_errores/

# Caché de grupos WhatsApp
/grupos.txt

# Agente de host — token propio y registro de procesos en ejecución, no se versionan
scripts/agentes-host/.env
scripts/agentes-host/procesos.json
